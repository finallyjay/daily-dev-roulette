# build output
dist/
.vercel/
.astro/

# deps
node_modules/

# secrets — never commit real tokens
.env
.env.local
.env.*.local

# agent / editor tooling (installed skills, local settings) — not app code
.claude/
skills-lock.json

# debug screenshots from spike scripts
spike/*.png

# os
.DS_Store
