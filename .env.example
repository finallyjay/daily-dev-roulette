# Optional: a daily.dev API token used to power the "Try the demo" button,
# so visitors without a Plus subscription can still play the roulette.
# Generate at https://app.daily.dev > Settings > API (requires Plus).
# Leave empty to disable the demo button. Real users sign in with their own token.
DAILY_TOKEN=
